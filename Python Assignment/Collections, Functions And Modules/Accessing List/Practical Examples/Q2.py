# 2. Write a Python program to find the length of a list using the len() function. 

# Define a list
my_list = [10, 20, 30, 40, 50]

# Find length  using the len() function.
print("The length of the list is:",len(my_list))


